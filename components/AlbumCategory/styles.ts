import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    margin: 10,
  },
  title: {
    color: "white",
    fontWeight: "bold",
    fontSize: 28,
    marginLeft: 10,
  },
});

export default styles;
